export const Types = {};

export const Actions = {};

const INITIAL_STATE = {
  initialTeste: [],
};

export default function inital(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return { ...state };
  }
}
